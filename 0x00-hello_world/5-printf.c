#include <stdio.h>
/**
 * main-starting pint of the program
 * Return:0 if the codes executes successfully
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
